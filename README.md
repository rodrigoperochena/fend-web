# Fend Web

A site built with static site generator Eleventy.

## Requirements

[NPM](https://npmjs.com)

## Installation

Open a terminal in the project folder and run:

`npm install`

Then run:

`npm run startBuild`